<%= form_for(@name) do |f| %>
  <% if @name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@name.errors.count, "error") %> prohibited this name from being saved:</h2>

      <ul>
      <% @name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <%= f.label :Time_Available %><br />
    <%= f.text_field :Time_Available %>
  </div>
  <div class="container">
    <%= f.label :Essay %><br />
    <%= f.text_area :Essay %>
  </div>
  <div class="form-actions">
    <%= f.submit %>
  </div>
<% end %>
